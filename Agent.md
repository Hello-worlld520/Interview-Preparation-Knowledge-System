# Agent

## Agent vs 普通大模型

* ### 自主性

  能够形成感知，规划，行动，再感知的闭环

  关于再感知：获取结果后将内容传回大模型，判断是否要再次行动

* ### 行动性

  能够自主调用工具

#### 自主调用工具的原理

* 用户输入内容到agent背后的大语言模型
* 大语言模型决定要使用什么工具，生成结构性调用
* 工具执行器通过外部API调用外部工具
* 传回数据到大模型

#### Agent的前世：为什么到25年才爆发agent？

* 根本原因是大模型的成熟，能够实现对话和调用等决策
* agent标准协议的逐渐成型
* agent生态逐渐完善

#### Agent的未来：agent生态的未来趋势？

事物的繁荣会促进标准的完善

* #### 工具使用的统一化：MCP

  MCP由三层构成

  * 首先是host层：使用MCP的应用提出请求
  * 其次是client层：负责连接和路由
  * 最后是server层：提供工具和能力

* #### Agent之间的协作和沟通：A2A

  每个agent都有一个名片，写明自己能干什么，怎么调用，如何通信

# Agent架构组成

* 记忆系统
* LLM
* 规划模块
* 工具系统

# ==记忆系统==

# Tools

Tools 是整个体系里最简单、最底层的概念，**它本质上是一个「按特定格式暴露给 LLM 的函数」**：普通函数是给程序员调用的，Tool 是给 LLM 调用的，所以必须给它配一份 LLM 看得懂的 schema（名字、描述、参数类型），否则 LLM 不知道它存在、也不知道怎么用。除了这层 schema 包装，它和普通函数没有本质区别，有明确的输入参数、明确的输出结果。

# Agent

AI做决策，调用工具完成任务

# Workflow 

人预设好的固定流程

#### Agent和Workflow是并列差异的关系 ，区别是谁来决定下一步

#### **Agent类似于自动驾驶，设计者只是在副驾驶设计了一些安全设置**

# **Agent的设计范式**

就是如何设计一个agent的做事流程

# **Agent的推理模式**

就是agent如何做事，**设计范式是公司的管理制度，推理模式是员工的干活方法**

* ### 范式一：ReAct（Reasoning + Acting）—— “边想边做”

  * 优点：
    * 灵活，可以根据实时状态调整决策
    * 避免冲动决策，提高决策质量

  * 缺点：
    * 每一个决策都是局部最优解，容易偏离主要目标，在任务中迷路

* ### 范式二：Plan-and-Execute（计划-执行）—— “三思而后行”

  * 优点：
    * 整体结构清晰，能够人工审核
    * 成本上，规划阶段需要强推理大模型，而执行阶段可以使用相对便宜的模型，混合模型配置可以节省成本

  注意！Plan-and-Execute是可以添加动态重规划机制的

  添加的优点：

  * 能够根据执行结果动态调整计划

  缺点：

  * 多了规划和重规划的LLM调用，成本和延迟都会增加
  * 初始计划方向错误的基础上，重规划并不能解决问题

* ### 范式三：**Reflection**/ Reflexion（基于上两种范式之上）

  #### Reflection

  它的做法是在 Agent 完成一步或者完成整个任务之后，再让一个 LLM（可以是同一个模型也可以是专门的评估模型）来判断做得好不好，结果是否符合预期。如果评估不通过，就重试或者换一种策略。

####      Reflexion

Reflection 有一个非常值得关注的变体叫 Reflexion。它和基础 Reflection 的区别在于，Reflexion 不只是简单地说「这个结果不好，重做一遍」，而是会生成一段具体的「反思总结」，记录下这次失败的原因和改进建议，然后把这段总结作为额外的上下文传给下一次尝试。

#### 动态重规划和Reflexion的区别

* 动态重规划是在执行过程中的随机应变，不改变agent做事的底层逻辑
* Reflection是在一个阶段任务完成后的反思总结，存入长期记忆，改变agent做事逻辑

### 范式总结

* 实际应用中是可以混用的，任务不复杂使用react，任务复杂需要统筹全局用Plan-and-Execute，关键步骤添加**Reflection**/ Reflexion质量把关

* 更常见的做法是**「Agentic Workflow」**，整体用 Workflow 框住主流程，在需要灵活处理的节点嵌入 Agent 能力。比如一个客服系统，意图识别 -> 知识检索 -> 回答生成这条主链路是固定的 Workflow，但「知识检索」这个节点内部可以用 Agent 来动态决定检索几轮、用哪些工具。这样既保住了整体可控，又有局部的灵活性，这是目前生产环境里最主流的做法。
* 先从最简单的 Workflow 开始，只有当你发现某个节点确实需要灵活决策、写死的逻辑无法覆盖所有情况时，才把那个节点升级成 Agent。

# 任务拆分

- **为什么拆**：解决LLM上下文窗口限制和长任务易出错的问题。
- **怎么拆**：分为**静态拆分**（写死流程，如流水线）和**动态拆分**（让LLM自己规划，即Plan-and-Execute模式）。
- **拆完做什么**：分析步骤依赖关系，把能并行的步骤并发跑，关键路径时间可以降 40% 到 60%。

# Multi-Agent（多Agent协作）

#### 为什么采用多Agent协作

*  context 窗口大小受限，复杂任务信息量一多就撑爆了
* 不同环节可以采用专业度更高的专才Agent

#### Multi-Agent 系统的组织方式

* 中心化

  由一个统一的调度者来分配任务、收集结果

* 去中心化

  Agent 之间自行协商、直接通信

工程中使用中心化比较多，调度逻辑清晰，责任归属明确，排查问题也更容易

#### 多Agent协作的标准模式

* 顺序流水线
* 并行扇出：一个调度者把多个独立子任务同时分发给不同的 Worker Agent，它们各自并行执行，最后由调度者收集汇总
* 辩论/评审模式

# Single-Agent 和 Multi-Agent如何选型？

在实际开发中，我会先选择用Single-Agent 把系统跑起来，当系统部分环节遇到瓶颈时，这种瓶颈主要分为两点，一是context window达到上限，二是遇到需要专业度更高的agent解决问题时，把这个环节单独交给一个work-Agent	

同时Multi-Agent又分为中心化和去中心化两种，在实际应用中中心化比较常用，因为它调度逻辑清晰，责任归属明确，排查问题也更容易

# Agent 的记忆压缩

* 摘要压缩
* 滑动窗口
* 重要性过滤
* 结构化抽取

通常**摘要压缩和滑动窗口**配合使用，滑动窗口负责控制对话历史的总长度上限，摘要压缩负责在历史被丢弃之前做一次提炼，把关键信息留下来

而二者都基于时间处理信息，此时就需要引入**重要性过滤**，根据重要性处理信息

* 也就是给每条记录打分
  * 规则打分：包含「决定」「确认」「需求」等关键词的记录加分，被后续对话引用次数多的加分，纯闲聊降分
  * LLM打分：重要性判断更精确，但每次都调用LLM开销大

* 另一种更激进的重要性过滤思路叫**观察遮蔽**
  * 它的做法不是删除低分内容，而是在构造 prompt 时选择性地「隐藏」某些历史条目。
  * 既节省了 token，又避免了不可逆的信息丢失。

**记忆压缩**也分为主动压缩和被动压缩

* 主动压缩是在Agent 在每一步执行完之后，主动判断哪些中间过程可以压缩。
* 被动压缩是等 context 快满了才开始压缩

**结构化抽取**

抽取关键字段形成结构化档案卡，不再以文本形式保存内容

不过需要开发者了解场景需求，制定合适的字段，且不同类型的任务所需的字段可能完全不同，字段通用性较低

## Prompt Caching

> **把重复发送给LLM的公共前缀（System Prompt、长文档、历史对话）缓存下来，下次请求时只传增量部分，LLM直接复用缓存内容。**

**本质**：用**存储空间**换**计算时间+Token费用**。

Prompt Caching 要和记忆压缩区分清楚，它是「计算层」的优化，对已经决定带进去的内容减少重复计算，和「信息层」的压缩是共存的互补关系，不是替代关系

# ==Agent框架==

